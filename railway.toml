[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health-live.php"
healthcheckTimeout = 120
preDeployCommand = "/usr/local/bin/pre-deploy.sh"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
